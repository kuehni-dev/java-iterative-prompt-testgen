SELECT P.ID,
       P.UUID,
       P.MOTIVE,
       P.INPUT_TOKENS,
       P.OUTPUT_TOKENS,
       CASE
           WHEN P.COMPLETED_AT IS NULL THEN NULL
           ELSE DATEDIFF(MILLISECOND , P.CREATED_AT, P.COMPLETED_AT) / 60000.0 END DURATION_MINUTES
FROM PROMPT P
ORDER BY DURATION_MINUTES DESC;
