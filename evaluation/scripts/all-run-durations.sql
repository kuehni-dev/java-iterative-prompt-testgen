SELECT R.UUID,
       R.D4J_PROJECT,
       R.D4J_BUG_ID,
       CASE
           WHEN R.COMPLETED_AT IS NULL THEN NULL
           ELSE DATEDIFF(MILLISECOND, R.CREATED_AT, R.COMPLETED_AT) / 60000.0 END DURATION_MINUTES
FROM RUN R
ORDER BY DURATION_MINUTES DESC;
