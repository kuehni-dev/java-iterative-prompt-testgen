ebean:
  ddl:
    generate: true
    run: false

datasource:
  db:
    username: ""
    password: ""
    url: jdbc:h2:file:./data/runs.h2;DB_CLOSE_ON_EXIT=TRUE;AUTO_SERVER=TRUE
    driver: org.h2.Driver
